{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @bardooo/api exec prisma generate"
  },
  "deploy": {
    "startCommand": "cd apps/api && pnpm exec prisma db push --skip-generate && pnpm start",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
